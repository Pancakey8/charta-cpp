fn thing (x : #a) -> () {
→ ∈ float = ? "Not float" print
            ↓
            "Float"
            print
}

fn stk () -> (... int) {
→ 1 2 3 4 5
}

fn main () -> () {
→ 3 thing 5.0 thing 'a' thing "a" thing stk thing
}
